apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: droit-ingress
  # annotations:
    # nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: level3ab.dbac.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: droit-level3ab-service
                port:
                  number: 5005