apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: droit-ingress
  # annotations:
    # nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: level5abbb.dbac.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: droit-level5abbb-service
                port:
                  number: 5009